import { build as buildCheckScopes } from './check_scopes';
import { build as buildCheckToken } from './check_token';
import { build as buildGetRoleList } from './get_role_data';

export default {
    buildCheckScopes,
    buildCheckToken,
    buildGetRoleList
} 